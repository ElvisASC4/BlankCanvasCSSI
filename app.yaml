runtime: python27
api_version: 1
threadsafe: yes

handlers:

#static directories should go ABOVE the catch all url (.*)
- url: /static/images
  static_dir: static/images
- url: /.*
  script: blog.app

libraries:
- name: webapp2
  version: "2.5.2"
- name: jinja2
  version: latest
